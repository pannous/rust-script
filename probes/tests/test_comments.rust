#!/usr/bin/env rust

// ^^ shebang line for Goo interpreter handled by comment code?
// This file tests Python-style hash comments
// old comment style
/*
 Multi-line comment block
 that should be foldable
 in the IDE
*/

#[test]
fn test_hash_comments_inline() {
    println!("OK"); // trailing comments now work with space before and after #
    println!("OK");
}

#[test]
fn test_hash_comments_with_variables() {
    let x = 1; // another trailing comment
    let y = 2; // works with semicolon too
    println!("{}", x + y); // prints 3
    assert_eq!(x + y, 3);
}
