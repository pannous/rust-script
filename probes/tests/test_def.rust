#!/usr/bin/env rust

#[test]
fn test_def_with_return_type() {
	fn meaning() -> i32 {return 42}
	# fn meaning() -> i32 {42} // alternative with implicit return
	# fn meaning(){return 42} // without explicit return type
	fn hello() {
		println!("Hello from def");
	}

	hello();
	println!("Meaning of life is {}", meaning());
}
