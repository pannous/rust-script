#!/usr/bin/env rust

#[test]
fn test_exit_macro_default() {
    // Test exit!() - default exit code 0
    // Note: In actual test context, exit!() would terminate the test
    // This test documents the exit! macro availability
    put!("Testing exit! macro");
    put!("exit!() with default exit code 0 is available");
}
