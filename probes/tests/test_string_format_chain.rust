#!/usr/bin/env rust

#[test]
fn test_string_format_chain() {
    let name = "Alice";
    let age = 25;
    let greeting = "Hello %s, you are %d years old" % name % age;
    eq!(greeting, "Hello Alice, you are 25 years old");
}
