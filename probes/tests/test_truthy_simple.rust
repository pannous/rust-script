#!/usr/bin/env rust

#[test]
fn test_zero_is_falsy() {
    if 0 != 0 {
        assert!(false, "0 should be falsy")
    }
}

#[test]
fn test_positive_is_truthy() {
    if 42 != 0 {
        // pass
    } else {
        assert!(false, "42 should be truthy")
    }
}

#[test]
fn test_empty_string_is_falsy() {
    if "" != "" {
        assert!(false, "empty string should be falsy")
    }
}

#[test]
fn test_nonempty_string_is_truthy() {
    if "hello" != "" {
        // pass
    } else {
        assert!(false, "non-empty string should be truthy")
    }
}

#[test]
fn test_true_is_truthy() {
    if true {
        // pass
    } else {
        assert!(false, "true should be truthy")
    }
}

#[test]
fn test_false_is_falsy() {
    if false {
        assert!(false, "false should be falsy")
    }
}

#[test]
fn test_negative_zero_is_falsy() {
    if -0 {
        assert!(false, "-0 should be falsy")
    }
}

#[test]
fn test_negative_number_is_truthy() {
    if -5 {
        // pass
    } else {
        assert!(false, "-5 should be truthy")
    }
}

#[test]
fn test_single_char_string_is_truthy() {
    if "x" {
        // pass
    } else {
        assert!(false, "single char string should be truthy")
    }
}

#[test]
fn test_whitespace_string_is_truthy() {
    if " " {
        // pass
    } else {
        assert!(false, "whitespace string should be truthy")
    }
}

#[test]
fn test_string_zero_is_truthy() {
    if "0" {
        // pass
    } else {
        assert!(false, "string '0' should be truthy")
    }
}

#[test]
fn test_i32_max_is_truthy() {
    if i32::MAX {
        // pass
    } else {
        assert!(false, "i32::MAX should be truthy")
    }
}

#[test]
fn test_i32_min_is_truthy() {
    if i32::MIN {
        // pass
    } else {
        assert!(false, "i32::MIN should be truthy")
    }
}
